---
title: maantano first
date: '2024-03-04'
tags: ['markdown', 'first']
draft: false
summary: My first blog post
---

My first blog post

## My first blog post

My first blog post `sum = parseInt(num1) + parseInt(num2)`

## My first blog post My first blog post

Some Javascript code

```javascript
hello my blog
```

Some Python code 🐍

```python
    N-QUEEN

    n = int(input())

    def chk(x):
        for i in range(x):
            if arr[x] == arr[i] or abs(x-i) == abs(arr[x]-arr[i]):
                return False
        return True

    def dfs(x):
        global result

        if x == n:
            result+=1
        else:
            for i in range(n):
                arr[x] = i
                if chk(x):
                    dfs(x+1)

arr = [0] * n
result = 0
dfs(0)
print(result)

```
